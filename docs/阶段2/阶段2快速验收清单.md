# 阶段 2 快速验收清单

> **状态：** ✅ 已实现（2026-08-30）  
> **详细版：** [阶段2实现报告.md](./阶段2实现报告.md)

---

## A. 方案认可（已完成）

- [x] 已验收 **阶段 1**（`verify_all` 全部通过）
- [x] 已读 [阶段2实现报告.md](./阶段2实现报告.md)
- [x] 认同三步：**整理需求故事 → 提取 JSON → 人工定稿**
- [x] `specification.yaml` 定稿（含 locking、deferred）
- [x] 修订记录已签方案认可

---

## B. 实现验收（已完成）

- [x] `specification.yaml` 定稿
- [x] `python -c "import yaml; yaml.safe_load(open('project/product-requirement/specification.yaml'))"` 无报错
- [x] 含字段：`entry` `exit` `steps` `feedback` `loop` `metrics` `acceptance` `locking`
- [x] `python implementation.py trials\case-01 --dry-run` 打印的三步与 yaml 一致
- [x] `python scripts\verify_stage2.py` 全绿（24 项）
- [x] `python scripts\verify_all.py --write-reports` 全绿
- [x] review.html「Loop 定义」面板可读 yaml（`/api/specification` 或内嵌 base64）
- [x] 实现报告状态为「已实现」

---

## C. 本阶段仍然不做（请确认）

- [x] （确认）没有改 `prompts/step1-story.md` —— 那是阶段 3  
- [x] （确认）没有真跑 Loop 出 json —— 那是阶段 4  
- [x] （确认）没有在 `implementation.py` 里实现锁定执行 —— 那是阶段 4  

---

## 一键自测

```bat
cd 课题申请-product-requirement-loop
python scripts\verify_all.py --write-reports
```

或双击根目录 `运行全部自测.bat`。
