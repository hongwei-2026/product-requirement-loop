# 阶段 1 快速验收清单

> **详细版：** [阶段1实现报告.md](./阶段1实现报告.md)  
> **自测说明：** [阶段1自测工作流说明.md](./阶段1自测工作流说明.md)

---

## 约 10 分钟勾表

### A. 一键自测（必做）

```bat
python scripts\verify_all.py --write-reports
```

- [ ] 最后一行显示 **`全部阶段通过`**
- [ ] `docs\自测\stage1-self-test-report.md` 里 **20 项全 ✓**

### B. 环境（必做）

- [ ] 已运行 **`setup阶段1环境.bat`**
- [ ] `project\.env` 含 **`LLM_PROVIDER=agnes`** 和有效 **AGNES_API_KEY**

### C. 执行器（必做）

- [ ] 存在 `project\implementation.py`
- [ ] 在 `project` 目录执行：`python implementation.py trials\case-01 --dry-run`  
      能看到三步：**整理需求故事 → 提取用户故事 JSON → 人工评审定稿**

### D. LLM 后路（了解即可）

- [ ] 知道改 `.env` 里 `LLM_PROVIDER` 可切 deepseek/openai，不必改代码
- [ ] 商业以后换 Key/厂商，不绑死 Agnes

### E. 文档

- [ ] 能在 `docs\阶段1\` 找到本清单和实现报告
- [ ] 在实现报告填 **执行人**、日期

---

## 通过后

进入 **阶段 2**：定 Loop 定义（`specification.yaml`）
